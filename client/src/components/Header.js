import { AppBar, Typography } from "@material-ui/core";

const Header = () => {
    <AppBar position="sticky">
        <Typography variant="h6">Socialnework</Typography>
    </AppBar>;
}

export default Header;